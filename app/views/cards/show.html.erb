<section id="container">
  <h1>This is <%= "Card№ #{@card.id}" %> </h1>
  <%= image_tag @card.picture.url(:medium) %>
  <table class="table">
    <tr>
      <th>Category</th>
      <th>Original Text</th>
      <th>Translated Text</th>
      <th>Created At</th>
      
    <tr>
      <tr>
        <td><%= @card.category.name %> </td>
        <td><%= @card.original_text %> </td>
        <td><%= @card.translated_text %> </td>
        <td><%= @card.review_date.strftime("%d/%m/%Y") %> </td> 
      </tr>
  </table>
  <br>
  <%= link_to "Home", root_path %>
  | <%= link_to 'Edit', edit_user_card_path(current_user, @card) %>
 </section>
