<section id="container">
  <h1>Список категорий</h1>
  <table border="1" cellpadding="8">
    <tr>
      <th>Категория</th>
      <th>Описание</th>
      <th colspan="2"></th>
    <tr>
    <% @categories.each do |f| %>
      <tr>
        <td><%= f.name %> </td>
        <td><%= f.about %> </td>
        <td><%= link_to 'Редактировать', edit_category_path(f) %></td>
        <td><%= link_to 'Удалить', category_path(f), method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
    <% end %>
  </table>
  <br>
  <%= link_to "Home", root_path %>
 </section>
